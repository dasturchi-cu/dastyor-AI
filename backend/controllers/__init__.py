"""
Controllers layer (HTTP entrypoints).

This package is intentionally thin: it exists to keep a stable place for future
extraction of route-handlers from `backend/routers/` without breaking imports.
"""

